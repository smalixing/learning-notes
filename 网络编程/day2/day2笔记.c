OSI:
	7层模型
TCP/IP：
	4层模型
	
IP：标识互联网中的某一台主机
PORT：区分通信的进程

socket:套接字编程
	TCP：流式套接字，面向连接，数据可靠、顺序、正确、可达
	UDP：数据报套接字，以报文形式传输，数据不可靠，不保证顺序、正确、可达，尽力传输
	原始套接字
	
函数返回值：如果只有0和-1那么这个返回值一般只用作函数是否正确调用的判断
			如果除了0和-1还有其它值，那这个函数的返回值一般而言会有其它用途，所以需要变量去接收
			
	

作业：
	1、完善客户端实现和服务器自由聊天.
	2、发送一张图片.

			
			

		