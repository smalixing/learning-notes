第1组代表的是文件的主人对该文件的权限
rw- r：可读  w：可写  -：不可执行
文件的硬链接数
文件的类型
ln是用来创建链接文件的
硬链接文件：在windows里面没有对应的概念
软链接文件：它对应着windows里面的快捷方式
以.开头的文件是隐藏文件

第2组代表的是文件所属组的用户对该文件的权限
第3组代表的是其他人对该文件的权限

文件的主人（属主）是谁？
组 group
进程的状态
S:sleep
R：ready（run)
家目录（主目录）
uid  user id
gid  group id
su 切换用户身份
ps进程是由bash进程生下来的
称bash是ps的父进程
重置密码
>在shell里面被称为输出重定向
<在shell中被称为输入重定向
即：把本应该从键盘获得的东西，从<号指定的文件中获得
|被称为管道，管道可以被想象成是现实生活中的橡胶水管

即：把本应该输出到屏幕的内容输出到>号所指定的文件中
*在shell里面，被称为通配符，它是匹配0和或者任意个任意字符

$*	  不包含扩展名的目标文件名称
$+	  所有的依赖文件，以空格分开，并以出现的先后为序，可能包含重复的依赖文件
$<	   第一个依赖文件的名称
$?	   所有时间戳比目标文件晚的的依赖文件，并以空格分开
 $@  目标文件的完整名称
$^	    所有不重复的目标依赖文件，以空格分开
$%    如果目标是归档成员，则该变量表示目标的归档成员名称

当前终端（shell）下的进程的信息

进程：程序的一次运行的实体  menu菜单  manual手册
linux主要有2个压缩算法，gzip   bzip2  抽取提取
打包 ， 不是压缩  解包  前台进程  后台进程 进程号
回音 回声
/代表根目录。父目录与子目录的分隔符
LInux的根目录，大致相当于windows的我的电脑
~代表主目录（家目录）家my home
改变当前所在的目录 （切换目录）
rmdir只能删除空目录。若i想删除非空目录，使用
rm -r 目录名
..代表上级目录（父目录）.代表当前目录  递归
mv重命名
linux命令的使用  关闭 boot启动
主目录 list列表 ls：显示当前目录下的文件或者文件夹（子目录）
cat：显示文本文件的内容v
quit 