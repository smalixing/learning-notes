```c
面试问题  
ftp文件传输过程中使用的协议：;

客户端断电，服务器怎么判断：心跳机制;

QT工作原理：事件驱动，信号与槽

信号：一个不完整的函数，只有声明，没有定义，由系统提供;

槽函数：可以由系统提供 也可以用户自己声明;

C++面向对象编程的核心思维：多态;

三次握手四次挥手：
    
文件的打开操作：fopen 更好的移植性 
		     open 不带缓存，效率慢
    
进程可以打开多少个文件：1021;

空洞文件：lseek fseek创建，听说可以提高迅雷的下载速度;

什么是进程和线程：
    进程：程序执行和资源管理的最小单元;
    线程：共享地址空间的多个任务;
        
网路编程的实质：通过操作相应 API 调度计算机硬件资源,并利用传输管道(网线)进行数据交换的过程
        
网络模型：OSI网络互连的七层框架（物理层、数据链路层、网络层、传输层、会话层、表示层、应用层）
    	 TCP/IP五层模型（物理层、数据链路层、网络层、传输层、应用层）
        
exit和_exit函数：
        _exit：直接终止进程，清除其使用的内存空间，销毁其在内核中的数据结构;
		exit：在调用eixt系统调用之前要检查文件的打开情况，将文件缓冲区中的内容写回文件;

什么是DBMS：数据库管理系统;

为什么sqlite3：它是一个轻量级的数据库，代码行数少，开源;

创建数据库：create table if not exists 表名;  

服务器模型：C/S模型；P2P模型；并发模型;
    
io多路复用的基本机制：即能够阻塞等待，非忙轮询装填，不浪费CPU等资源，也能同一时刻监听多个IO请求的机制;
    
C/S，B/S：客户端/服务器模式；浏览器/服务器模式;
    
程序和进程的区别：
    程序：是静态的，它是指令的有序集合，没有执行概念;
	进程：是动态的，它是程序执行的过程，包括创建，调度，消亡;

进程的优缺点：
    优点：每个进程互相独立，不影响主程序的稳定性，子进程崩溃也没影响;
    缺点：因为地址空间私有，进程间上下文切换时，系统开销比较大；
线程的优缺点：
    优点：1.大大提高了任务切换的效率
    	 2.避免了额外的TLB & cache的刷新
    	 3.线程间通信容易（全部变量就可以实现数据交换和共享）
    缺点：多个线程共同访问共享对象时需要引入同步和互斥锁
note:线程间同步:
	P/V操作：（生产者和消费者）p申请资源，信号量减1；V操作信号量加1;
        
fork和vfork的区别：
    fork：1.子进程拷贝父进程的数据段，代码段; 
    	  2.父子进程的执行次序不确定;
    vfork：1.子进程与父进程共享数据段， 
           2.保证子进程先运行，在调用exec或exit之前与父进程数据是共享的，在它调用exec或exit 			     之后父进程才可能被调度运行

如何实现同一时刻做多个事件：采用并发的思想，使用多进程或者多线程;

临界资源：一次仅允许一个进程或线程使用的共享资源;

管道的特点：速度慢,容量有限,只能用于具有亲缘关系的进程间通信。

管道：把从一个进程连接到另一个进程的一个数据流称为一个管道。

临界区：临界区指的是一个访问共用资源（例如：共用设备或是共用存储器）的程序片段，而这些共用资源又				无法同时被多个线程访问的特性

竞态：“竞态”就是在多线程的编程中，你在同一段代码里输入了相同的条件，但是会输出不确定的结果的情况

快排的基本思想：通过一趟排序将待排序列分割成两部分，其中一部分记录的关键字均比另一部分记录的关键字小。之后分别对这两部分记录继续进行排序，以达到整个序列有序的目的。

查找算法效率最高的是：哈希查找

什么是B树和红黑树和查找二叉树和平衡二叉树：
    B树：
    红黑树：
    查找二叉树：
    平衡二叉树：

求图的最短路径：
    深度或广度优先搜索算法，
    费罗伊德算法，
    迪杰斯特拉算法，
    Bellman-Ford 算法

程序可以创建多少个进程：65535

服务器最多可以连接多少个客户端：65535

ARM：是一个公司的名字，成立于1990年11月，设计精简指令集(RISC)处理器内核，授权ARM内核给生产和销售半导体的合作伙伴，也提供基于ARM架构的开发设计技术;

C++重载运算符：只能满足基本的数据类型运算，为了打破这种限制，就将运算符重载，进而实现非基本数据类型数据运算

什么是引用：取别名，引用必须在创建时被初始化...

C++支持的编程模型：程序模型(面向过程)；抽象数据类型模型；面向对象模型

什么是封装：“封装，即隐藏对象的属性和实现细节，仅对外公开接口，控制在程序中属性的读和修改的访问级别；将抽象得到的数据和行为（或功能）相结合，形成一个有机的整体，也就是将数据与操作数据的源代码进行有机的结合，形成“类”，其中数据和函数都是类的成员。（保护源码，开放接口）

什么是继承，目的是什么：

什么是多态：

什么是隐藏：

什么是覆盖：

QT中信号与槽关联注册有哪些方式：connect函数，lamda表达式，UI设计界面的Signals & Slots Editor，
    					   UI设计界面中可以将可以发出信号的控件转到槽

信号与槽函数参数的特点：1.一个信号可以连接多个槽
                    当信号发射时，会以不确定的顺序一个接一个的调用各个槽
                    2.多个信号可以连接同一个槽
                    即无论是哪一个信号被发射，都会调用这个槽。
                    3.信号直接可以相互连接
                    发射第一个信号时，也会发射第二个信号。
                    4.连接可以被移除
                    信号与槽关联是否可以解除：可以解除，使用disconnect函数

QT当中内存回收机制：使用new和delete时，内存在堆中分配。堆内存空间必须通过delete完全释放,以防止内存泄漏。只要不delete ,分配在堆上的对象可以一直存活下去。
				 栈是系统自动分配管理的,局部变量就是来自于栈区, 只要超出了作用域的栈区数据就会被自动回收。
    			 所有从QObject继承出来的子对象的内存管理都转移给了父对象
                 使用new在堆上分配内存
                 子对象可自动被父对象删除内存
                 手动删除不会引起二次删除，子对象删除时会通知父对象
                 没有父对象的对象需要手工删除
                 没有父对象的对象- -般分配在栈区，这样可以有效避免内存泄漏
                 Qt没有自动回收站的机制,需要关注父子关系及其功能
```

